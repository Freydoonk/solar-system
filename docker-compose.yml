services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: solar-system-dev
    image: solar-system:dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true